- if user.errors.any?
  - unless user.valid?
    - if errors = user.errors.full_messages_for(column.to_sym)
      %ul.information-registration__base__main-box__click-area__error-font
        - errors.each do |error|
          %li=error